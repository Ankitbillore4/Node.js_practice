<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>random</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="container mx-auto px-4 py-8">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% data.forEach(function(person) { %>
                <div class="bg-white shadow-lg rounded-lg overflow-hidden transform transition duration-500 hover:scale-105">
                    <div class="p-6">
        <h1 class="text-2xl font-bold mb-2"><%= person.name%></h1>
        <h1 class="text-xl font-semibold mb-1"><%= person.age%></h1>
        <h1 class="text-lg mb-1"><%= person.add%></h1>
    <% }); %>></div>
    
</body>
</html>